# Build, publish, and deploy utilities

Multiple scripts (bat for windows, sh for posix), to build the binaries for your deployment.

When binaries are produced, they will be placed in the ".dist" folder.